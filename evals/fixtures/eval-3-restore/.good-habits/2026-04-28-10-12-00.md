# Q&A 记录 - 2026-04-28-10-12-00

> 由 good-habits-skill 自动生成

## 用户问题

加一个 POST /login 接口，账号密码登录，返回 JWT token。

## 实现方案

`src/server.ts` 里挂一个 `POST /login` handler，做了最小可用版本（占位返回 stub token），等接到真实用户表后再补 bcrypt + 数据库查询。

## 修改的文件及内容摘要

- src/server.ts: 新增 POST /login，返回固定 stub token

## 备注

- 当前 token 是写死的占位值，**生产前必须接真实的密码校验和 JWT 签发逻辑**。
- 暂未挂中间件保护，Authorization 头还没生效。
