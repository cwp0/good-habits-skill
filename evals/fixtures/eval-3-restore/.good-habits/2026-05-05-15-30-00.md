# Q&A 记录 - 2026-05-05-15-30-00

> 由 good-habits-skill 自动生成

## 用户问题

把日志库从 winston 换成 pino，要求改完之后所有现有接口的日志格式不变。

## 实现方案

经过习惯二的方案对比后选了 pino + pino-pretty 本地开发用，生产用纯 pino + JSON。在 `src/log.ts` 里包了一层 logger，业务代码调用 logger.info / logger.warn 不用改。

## 修改的文件及内容摘要

- src/log.ts: 新增统一 logger 入口
- src/server.ts: 替换原 winston 调用为新 logger

## 备注

- pino 输出默认是 NDJSON，运维侧的 ELK 索引需要确认能不能直接吃。
- 老的 winston transport 暂时没删，下个版本再清理。
